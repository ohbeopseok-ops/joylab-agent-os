# JoyLab Agent OS — V0.5.3 FROZEN CERTIFIED BASELINE

Freeze date: 2026-08-29  
Baseline commit: `9e9cb2cfc75aaf6430225c729522c72d1306f71a`  
Release anchor: `release/v0.5.3-frozen`

## Status

```text
VERSION: V0.5.3
STATE: FROZEN
CERTIFICATION: PASS
GOLD: GOLD_001 ~ GOLD_070 CERTIFIED
PYTHON: 3.11 / 3.12 / 3.13 GREEN
```

## Certified trust stack

```text
Code
  -> Python Matrix CI
  -> Regression
  -> Gold Registry
  -> JSON Schema
  -> EVS Integrity
  -> EVG Integrity
  -> Approval Audit
  -> V05CertificationGate
  -> PASS / FAIL
```

The baseline is release-eligible only when every required check passes.

## Frozen capabilities

- Governed Skill Registry and lifecycle gates
- Append-only Experience Logger
- EvidenceBuilder
- immutable EVS snapshots with SHA-256
- MemoryRouter and MemoryWritePolicy
- Core8 E2E adapter
- SkillCandidateGenerator
- SkillCurator
- Candidate Diff
- Approval Audit Log
- EvidenceGraph
- immutable EVG snapshots with SHA-256
- AI Power adapter
- NVDA Event adapter
- EPS Revision adapter
- Master Ranking adapter
- AdapterRegistry
- DomainPluginRegistry
- governed Gold Case JSON Registry
- Unified V0.5 Certification Gate

## Certified Gold range

`GOLD_001 ~ GOLD_070`

All entries are registered as `CERTIFIED` at this baseline.

## Governance invariants

1. CERTIFIED skills cannot be modified in place.
2. Candidate promotion requires evidence and validation.
3. Critical UNKNOWN values cannot be silently coerced into numeric evidence.
4. EPS Revision <= -10% preserves BUY_BLOCK semantics.
5. Ranking #1 never bypasses execution, portfolio, or human approval gates.
6. EVS and EVG artifacts are integrity-verifiable.
7. Approval history remains attributable and append-only.
8. A failed Certification Gate blocks release.
9. New Gold Cases enter as CANDIDATE and may be promoted only after evidence is GREEN.
10. Future V0.6+ work must not redefine this baseline.

## Freeze boundary

The exact executable baseline is the commit:

`9e9cb2cfc75aaf6430225c729522c72d1306f71a`

The branch:

`release/v0.5.3-frozen`

is an anchor to that exact commit and must not be advanced for future development.

## Post-freeze rule

All new work starts from `main` on a new feature branch.

V0.6 changes may:
- extend adapters
- add persistence
- add scheduling
- add runtime orchestration

V0.6 changes may not silently mutate:
- V0.5.3 Gold evidence
- frozen EVS/EVG contracts
- certification semantics
- historical approval provenance

Any intentional compatibility break requires an explicit new version and migration record.
